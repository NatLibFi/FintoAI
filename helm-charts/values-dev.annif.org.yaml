timezone: "Europe/Helsinki"
cluster: 'test'  # TODO Drop this when hostname is transferred

annif:
  version: 'latest'
  resources:
    limits:
      cpu: 1
      memory: 10Gi
  modelDataStorage: 8Gi
