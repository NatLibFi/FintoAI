# This file contains configuration that is common to all instances.

timezone: "Europe/Helsinki"

nginx:
  version: '1.20'
  baseImage: 'ubi8'
  # TODO Increase storage when project quota allows
  buildTriggers:
    - type: ImageChange
      imageChange: {}
    - type: ConfigChange

annif:
  version: '0.57'
  resources:
    limits:
      cpu: 1
      memory: 26Gi
  modelDataStorage: 25Gi
