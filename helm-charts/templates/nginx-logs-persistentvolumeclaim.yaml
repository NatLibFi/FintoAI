kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.instance }}-logs
  labels:
    app.kubernetes.io/instance: {{ .Values.instance }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.logsStorage }}
