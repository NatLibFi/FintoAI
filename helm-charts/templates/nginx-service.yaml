apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.instance }}-nginx
  labels:
    app.kubernetes.io/instance: {{ .Values.instance }}
spec:
  selector:
    app: {{ .Values.instance }}-nginx
  ports:
  - port: 8080
