kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Release.Name }}-textract
  labels:
    app.kubernetes.io/name: textract
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: textract
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: textract
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: textract
          image: >-
            image-registry.openshift-image-registry.svc:5000/annif/{{ .Release.Name }}-textract:latest
          livenessProbe:
            exec:
              command: ["curl", "--fail", "localhost:{{ .Values.textract.port }}/status"]
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 60
          resources:
            {{ toYaml .Values.textract.resources | nindent 12 }}
          env:
            - name: PORT
              value: {{ quote .Values.textract.port }}
            - name: TZ
              value: {{ .Values.timezone }}
          ports:
            - containerPort: {{ .Values.textract.port }}
              protocol: TCP
